management:
  endpoints:
    web:
      exposure:
        include: "8"

  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true
  endpoint:
    health:
      probes:
        enabled: true
